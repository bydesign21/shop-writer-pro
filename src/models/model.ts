export enum UserRole {
  USER = 'user',
  EMPLOYEE = 'employee',
  ADMIN = 'admin'
}

export enum TicketStatus {
  RESOLVED = 'resolved',
  OPEN = 'open',
  PENDING = 'pending',
  CANCELED = 'canceled',
  REFUNDED = 'refunded'
}
